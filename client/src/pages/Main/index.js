import React, { Component } from "react";

class Main extends Component {
  handleClick(e) {
    e.preventDefault();
    // window.open(About);
  }

  render() {
    return (
      <div>

        {/* <Header />


        <AboutContent />
        <Footer /> */}
        HOME PAGE
      </div>
    );
  }
}

export default Main;
