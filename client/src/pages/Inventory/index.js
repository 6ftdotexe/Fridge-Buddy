import React, { Component } from "react";

class Inventory extends Component {
  handleClick(e) {
    e.preventDefault();
    // window.open(About);
  }

  render() {
    return (
      <div>
        {/* <Header />
        <AboutContent />
        <Footer /> */}
        INVENTORY PAGE
      </div>
    );
  }
}

export default Inventory;
