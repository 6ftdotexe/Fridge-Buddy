import React, { Component } from "react";

class About extends Component {
  handleClick(e) {
    e.preventDefault();
    // window.open(About);
  }

  render() {
    return (
      <div>
        <p>ABOUT PAGE</p>
       
      </div>
    );
  }
}

export default About;
